"""Top-level package for the local LLM + MCP web research agent.

This package provides:

- A local LLM client (for example, via Ollama or vLLM) using an OpenAI-compatible endpoint.
- MCP server management for Playwright MCP and Chrome DevTools MCP.
- An agent loop that uses tools to browse and analyze web pages.
- A CLI entrypoint for interactive usage.
"""
